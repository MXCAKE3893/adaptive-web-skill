---
name: adaptive-web
description: >
  HTML/CSS/JavaScript製のWeb UIを画面サイズと入力方法に適応させるためのSkill。
  レスポンシブナビゲーション、複数ペイン、可変列数、タッチ・マウス・
  キーボード操作、アクセシビリティを扱う。
---

# Adaptive Web UI

## 基本方針

- 既存のデザインシステム、フレームワーク、ルーター、ビルド構成を維持する。
- 新しいフレームワークへの移行を前提にしない。
- デバイス名やUser-Agentではなく、利用可能な幅・高さと入力特性に適応する。
- CSSで解決できるレイアウトにJavaScriptを使わない。
- セマンティックHTMLと自然なDOM順序を維持する。見た目だけの並べ替えで読み上げ順序やフォーカス順序を壊さない。
- ウィンドウのリサイズ、画面回転、ズームで選択・入力・スクロール状態を不要に失わせない。
- 非対応ブラウザーでも主要な内容と操作を利用できるようにする。

## 前提確認

1. 対象画面、既存コンポーネント、CSS設計、ルーティング、テスト実行方法を確認する。
2. 対象ブラウザーと最低対応バージョンを確認する。不明で採用APIの判断に必要な場合だけ質問する。
3. 既存の未コミット変更を確認し、無関係な変更を上書きしない。
4. 実験的なAPIや新規依存関係が必要なら、対応状況を公式資料で確認し、導入前に承認を得る。

## 作業手順

依頼範囲に必要なステップだけを実施する。

### Step 1: 現状の表示と操作を確認する

- 狭幅・中幅・広幅で現状を記録する。初期確認用の例は幅360px、768px、1280pxとするが、固定の設計基準にはしない。
- 高さの小さい横向きウィンドウ、長い文字列、空状態、読み込み中、エラー状態も確認する。
- 既存のスクリーンショットテストとE2Eテストを優先する。新規テストツールの導入は先に相談する。
- テスト環境の日時、データ、フォント、アニメーションを可能な範囲で固定する。
- 変更前の参照画像を保存し、変更後との差分を確認する。参照画像はユーザーの確認・承認なしに更新しない。

### Step 2: 基本レイアウトを適応させる

- viewport設定が必要なら `width=device-width, initial-scale=1` を設定する。ズームを禁止しない。
- 固定幅を見直し、Grid、Flexbox、max-inline-size、相対単位を使う。
- ページ全体の切り替えにはメディアクエリを使い、配置先の幅に依存する再利用コンポーネントにはコンテナクエリを検討する。
- ブレークポイントは端末名ではなく、実際に内容が収まらなくなる幅から決める。
- Grid/Flexの子要素には必要に応じて `min-inline-size: 0` を設定する。
- 長いURLや文字列には折り返しを設ける。表やコードなど横スクロールが必要な内容は、その領域だけをスクロール可能にする。
- ページ全体への `overflow-x: hidden` でレイアウト不具合を隠さない。
- 画像は縦横比を維持してコンテナに収める。意図せず重要部分をトリミングしない。

コンテナクエリは、クエリ対象の親をコンテナとし、その子孫に適用する。以下の値は例であり、実際の内容に合わせて調整する。

```css
.card-region {
  container-type: inline-size;
}

.card {
  display: grid;
  gap: 1rem;
}

@container (min-width: 36rem) {
  .card {
    grid-template-columns: minmax(0, 1fr) minmax(0, 2fr);
  }
}
```

### Step 3: ナビゲーションを適応させる

- 狭幅では下部ナビゲーションまたは開閉式メニュー、広幅では側面または上部ナビゲーションを検討する。既存の情報設計を優先し、機械的に置き換えない。
- 移動先には `a`、開閉などの操作には `button` を使う。
- 現在のページは `aria-current="page"` で示す。ナビゲーション領域には識別可能なラベルを付ける。
- メニュー開閉ボタンには `aria-expanded` と `aria-controls` を設定する。
- 非表示のメニュー内にキーボードフォーカスが入らないようにする。opacityだけで非表示にしない。
- モーダルとして開くメニューでは、適切な初期フォーカス、背景への操作抑制、Escapeによる終了、閉じた後のフォーカス復帰を実装する。
- 通常の非モーダルナビゲーションにはフォーカストラップを適用しない。
- 同じナビゲーションを複製するより、単一のDOMをCSSで再配置する方法を優先する。
- 固定バーは本文やフォーカス対象を覆わないよう領域を確保する。必要に応じてsafe-area-insetを考慮する。

### Step 4: 複数ペインを導入する

関連する画面がある場合だけ、次の構成を検討する。

- 一覧・詳細: 一覧の選択項目に対応する詳細を表示する。
- メイン・補助: 主内容と、それを補う情報や操作を表示する。

実装要件:

1. 狭幅では単一ペイン、十分な幅がある場合は複数ペインを表示する。
2. 選択状態を単一の情報源で管理し、リサイズで初期化しない。
3. ページ遷移に相当する詳細画面は既存ルーターとURL設計に統合する。戻る・進む・直接アクセス・再読み込みを確認する。
4. 純粋に表示幅が変わっただけでは履歴エントリーを追加しない。
5. 未選択時の詳細プレースホルダーを用意する。
6. 狭幅で詳細を開いた際は適切な見出しなどへフォーカスを移す。戻った際は可能な限り元の一覧項目へ戻す。
7. 広幅で一覧が表示されている場合、狭幅用の「一覧へ戻る」操作は必要性を再評価する。
8. リサイズでフォーカス中の要素を非表示にする場合は、表示される適切な要素へフォーカスを移す。
9. 画像・動画など広い表示領域が重要な内容に、依頼なしで複数ペインを強制しない。

### Step 5: 一覧の列数を適応させる

- カードの最小可読幅を決め、CSS Gridで利用可能な幅に応じて列数を変える。
- テキスト一覧や表を無条件にカードグリッドへ変更しない。
- DOM順序を保ち、見た目と読み順が食い違う配置を避ける。
- 大量データでは既存のページネーションや仮想化を維持する。CSS Grid自体に仮想化機能があるとは扱わない。

```css
.card-list {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(min(100%, 16rem), 1fr));
  gap: 1rem;
}
```

`16rem` は例とし、内容に応じて調整する。

### Step 6: 入力方法とアクセシビリティに対応する

- 全操作をキーボードで実行可能にし、見えるフォーカス表示を維持する。
- hoverでしか表示・実行できない主要操作を作らない。
- `hover` / `pointer` のメディアクエリは入力特性に応じた追加調整に使う。端末種別の判定には使わない。
- 複数の入力機器を持つ環境を考慮し、精密ポインターが検出されてもタッチ操作やキーボード操作を無効化しない。
- 操作対象は十分なサイズと間隔を確保する。プロジェクト基準がなければ44 CSS px程度を設計上の目安とし、規格の一律必須値とは扱わない。
- 入力欄にはラベルを付け、適切なtype、autocomplete、必要に応じたinputmodeを使う。
- ソフトウェアキーボード表示時も入力欄と送信操作へ到達できることを確認する。
- `prefers-reduced-motion: reduce` では不要な移動アニメーションやスムーズスクロールを抑制する。
- 色だけに依存して状態を伝えない。コントラストを確認する。
- ズーム、文字サイズ拡大、長い翻訳文で内容や操作が欠落しないことを確認する。

### Step 7: スクロール時のバー表示を必要に応じて調整する

- 自動的な非表示は要件がある場合だけ導入する。sticky配置で十分なら、それを優先する。
- 導入する場合は「下方向で隠す・上方向で表示する」などの規則を明確にする。
- バー内にフォーカスがある場合やメニューが開いている場合は隠さない。
- 実際のスクロールコンテナを監視し、不要にwindowへイベントを登録しない。
- スクロール処理では頻繁な同期レイアウト計算を避ける。イベントリスナーは不要になった時点で解除する。
- 画面ごとの状態を分離し、画面遷移後にバーが隠れたままにならないようにする。
- sticky/fixed要素でアンカー移動先やフォーカス表示が隠れないようにする。

## 最終検証

- プロジェクト既存のビルド、lint、型チェック、単体テスト、E2Eテストを必要に応じて実行する。
- 代表幅だけでなく、各ブレークポイントの直前・直後と中間幅も確認する。
- 縦向き・横向き、低いウィンドウ、拡大表示で不要なページ全体の横スクロールがないことを確認する。
- キーボード操作、メニュー開閉、フォーカス復帰、URL直接アクセス、履歴操作を確認する。
- リサイズ後も入力値と選択状態が維持されることを確認する。
- スクリーンショット差分を確認し、承認なしに参照画像を更新しない。
- 自動アクセシビリティ検査だけで完了とせず、キーボード操作も手動確認する。
- 実機で確認できないタッチ操作やソフトウェアキーボードの挙動は、未検証として報告する。
- 実行できなかった検証と理由を明記する。未実行のテストを成功と報告しない。

## 参考資料

利用するAPIの対応状況は、実装時に対象ブラウザーと照合する。

- Container queries: https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Containment/Container_queries
- Responsive design: https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/CSS_layout/Responsive_Design
- CSS Grid: https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout
- Pointer media feature: https://developer.mozilla.org/en-US/docs/Web/CSS/@media/pointer
- Reduced motion: https://developer.mozilla.org/en-US/docs/Web/CSS/@media/prefers-reduced-motion
- Dialog pattern: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/
